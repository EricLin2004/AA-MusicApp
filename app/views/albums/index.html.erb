<h1> Albums </h1>

<% @albums.each do |album| %>
<ul>
<li><%= link_to album.name, album_url(album) %></li>
</ul>
<% end %>

<%= link_to "Create New Album", new_album_url %>
<br>
<%= link_to "Sort by name", albums_url + "?order=name"%>
<br>
<%= link_to "Sort by band name", albums_url + "?order=band_name"%>